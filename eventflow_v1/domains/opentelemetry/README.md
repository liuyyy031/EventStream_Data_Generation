# OpenTelemetry placeholder

This directory reserves the EventFlow domain interface only. Trace/span event
semantics, parent-link rules, text alignment, reference distributions, and
domain validation are intentionally deferred. It is not a reduced-quality
implementation and must not be used to generate data yet.

