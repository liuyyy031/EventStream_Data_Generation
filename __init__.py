"""Current domain-neutral event-stream data generation package.

Only ``generation_core`` and ``domain_packages`` belong to the active runtime.
Historical generators are isolated under ``data_generation.legacy``.
"""

